# Step-by-Step Walkthrough for SSH and `tmux` Setup for `bwa mem` Command

## 1. SSH into Your Azure VM
Use the `az ssh vm` command to log into your VM:
```bash
az ssh vm --name your-vm-name --resource-group your-resource-group